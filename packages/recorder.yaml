recorder:
  exclude:
    domains:
      - input_select
      - media_player
      - automation
      - input_slider
      - rest_command
      - camera
      - notify
      - scene
      - light
      - tts
      - device_tracker
      - mqtt
      - script
         
#  db_url: !secret recorder_db_url
  purge_interval: 2
  purge_keep_days: 5
